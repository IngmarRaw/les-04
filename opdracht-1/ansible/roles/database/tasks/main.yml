---
- name: Installeer MySQL server
  ansible.builtin.apt:
    name: "{{ mysql_package }}"
    state: present
  notify: Restart mysql

- name: Installeer PyMySQL voor Ansible MySQL modules
  ansible.builtin.apt:
    name: "{{ mysql_python_package }}"
    state: present

- name: Zorg dat MySQL gestart en enabled is
  ansible.builtin.service:
    name: "{{ mysql_service_name }}"
    state: started
    enabled: true

- name: Configureer MySQL bind-address
  ansible.builtin.lineinfile:
    path: /etc/mysql/mysql.conf.d/mysqld.cnf
    regexp: '^bind-address\s*='
    line: "bind-address = {{ mysql_bind_address }}"
  notify: Restart mysql

- name: Flush handlers zodat MySQL herstart is voor database/user configuratie
  ansible.builtin.meta: flush_handlers

- name: Maak applicatiedatabase aan
  community.mysql.mysql_db:
    name: "{{ mysql_database_name }}"
    state: present
    login_unix_socket: /var/run/mysqld/mysqld.sock

- name: Maak MySQL gebruiker aan voor webserver
  community.mysql.mysql_user:
    name: "{{ mysql_user_name }}"
    password: "{{ mysql_user_password }}"
    host: "{{ mysql_user_host }}"
    priv: "{{ mysql_user_privileges }}"
    state: present
    login_unix_socket: /var/run/mysqld/mysqld.sock
  no_log: true
  notify: Restart mysql